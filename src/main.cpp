#include <iostream>

#include "matrix.hpp"

int main() {
    // Matrix2i a({1, 2, 3, 4});
    // Matrix2i b({3, 4, 5, 6});
    // std::cout << ((a + 2) == b) << '\n';

    // Matrix2f c;
    // Matrix2f d({1.5f, 1.5f, 1.5f, 1.5f});
    // EXPECT_EQ(c + 1.5, d);
}